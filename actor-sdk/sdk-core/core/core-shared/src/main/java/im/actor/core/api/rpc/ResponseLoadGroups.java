package im.actor.core.api.rpc;
/*
 *  Generated by the Actor API Scheme generator.  DO NOT EDIT!
 */

import im.actor.runtime.bser.*;
import im.actor.runtime.collections.*;
import static im.actor.runtime.bser.Utils.*;
import im.actor.core.network.parser.*;
import org.jetbrains.annotations.Nullable;
import org.jetbrains.annotations.NotNull;
import com.google.j2objc.annotations.ObjectiveCName;
import java.io.IOException;
import java.util.List;
import java.util.ArrayList;
import im.actor.core.api.*;

public class ResponseLoadGroups extends Response {

    public static final int HEADER = 0x252;
    public static ResponseLoadGroups fromBytes(byte[] data) throws IOException {
        return Bser.parse(new ResponseLoadGroups(), data);
    }

    private List<ApiGroup> groups;

    public ResponseLoadGroups(@NotNull List<ApiGroup> groups) {
        this.groups = groups;
    }

    public ResponseLoadGroups() {

    }

    @NotNull
    public List<ApiGroup> getGroups() {
        return this.groups;
    }

    @Override
    public void parse(BserValues values) throws IOException {
        List<ApiGroup> _groups = new ArrayList<ApiGroup>();
        for (int i = 0; i < values.getRepeatedCount(2); i ++) {
            _groups.add(new ApiGroup());
        }
        this.groups = values.getRepeatedObj(2, _groups);
    }

    @Override
    public void serialize(BserWriter writer) throws IOException {
        writer.writeRepeatedObj(2, this.groups);
    }

    @Override
    public String toString() {
        String res = "tuple LoadGroups{";
        res += "}";
        return res;
    }

    @Override
    public int getHeaderKey() {
        return HEADER;
    }
}
